#include "AbstractBase.h"

class Factory
{
	public:
		static Operation * getOperation(std::string oper)
		{
				switch(oper)
				{
						case "+":
							return new OperationAdd() 
						case "-":
							return new OperationSub();
						default:
							return NULL;
				}
		}
}

